services:
  - type: web
    name: maison-luxe
    runtime: python
    rootDir: fashion
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
